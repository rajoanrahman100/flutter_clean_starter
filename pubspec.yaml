name: flutter_scaffold
description: A CLI tool to scaffold Flutter projects with Clean Architecture.
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

executables:
  flutter_scaffold: flutter_scaffold
